import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "SUBSENSE — Real-Time Predictive Mine Subsidence Intelligence",
  description: "Live geotechnical sensor telemetry, spatial-temporal precursor analysis, and debounced incident management for underground mining safety.",
  icons: {
    icon: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
        <link
          href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600;700&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>{children}</body>
    </html>
  );
}
